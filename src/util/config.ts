/** @internal */
export const config = {
  apiUrl: 'http://localhost:3000', //'http://194.195.246.128:3000', //ip of the api server
  eventDelay: 100, //the delay is required to gather some time to update changes
  maxListenerCount: 0, //disables max listener warning
};

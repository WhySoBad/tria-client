import { RequestManager } from './RequestManager.class';

/* type Requests = 'get/%uuid';

export class UserRequest extends RequestManager<Requests> {
  constructor() {
    super({ suburl: 'user' });

    this.addRequest('get/%uuid', 'GET');
  }
}
 */

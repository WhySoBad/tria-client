import { ChatSocketConstructor } from '../types/ChatSocket.types';
import { BaseSocket } from './BaseSocket.class';
export declare class ChatSocket extends BaseSocket {
    constructor(props: ChatSocketConstructor);
}
